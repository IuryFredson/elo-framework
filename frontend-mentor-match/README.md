# Frontend Mentor Match

Interface React/Vite da instância Mentor Match do Elo Framework.

```bash
npm install
npm run dev
```

O frontend inicia em `http://localhost:5175` e usa `http://localhost:8082` como API. Defina
`VITE_API_URL` para alterar o endereço do backend.
