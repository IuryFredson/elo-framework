# Profile de desenvolvimento: recria o schema a cada inicialização para que o
# DevDataSeeder popule um banco limpo. Ative com SPRING_PROFILES_ACTIVE=dev.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
